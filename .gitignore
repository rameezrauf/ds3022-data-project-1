# macOS system files
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env

# Logs (only runtime logs, not source-controlled configs)
logs/*.log
dbt/logs

# Data & databases (too large for git, can be rebuilt with scripts)
data/**/*.duckdb
data/**/*.parquet
data/raw/
data/tmp/
data/duckdb/

# Keep CSV outputs (grading artifacts) but ignore raw/full CSV data
# Don’t ignore all CSVs — only large raw ones
data/raw/**/*.csv
data/tmp/**/*.csv

# DBT build artifacts
dbt/target/
dbt/dbt_packages/
dbt/.user.yml

# Jupyter
.ipynb_checkpoints/